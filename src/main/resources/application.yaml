
spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/students_dairy
    username: root
    password: secret
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
      format_sql: true

